# Generated at setup/run time — secrets and local runtime state. Never commit.
.env
keys/
github-app-private-key.pem
.kube/
data/
.webhook-relay.pid
.webhook-relay.log

# Test-user logins for verify-scopes.sh — never committed.
.creds/
